{
  "name": "@drobe/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(eslint not configured yet)\"",
    "clean": "rm -rf dist node_modules .turbo"
  },
  "dependencies": {
    "@drobe/db": "workspace:*",
    "@drobe/shared": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.0.0",
    "fastify-plugin": "^5.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
